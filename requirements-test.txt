hypothesis>=6.0.0
pdfplumber>=0.11.0
model2vec>=0.1.1
litellm>=1.20.0
mammoth>=1.6.0
sqlite-vec>=0.1.0
headroom-ai>=0.27.0
duckdb>=1.0.0
fastapi>=0.100.0
imagehash>=4.3.0
pytest-asyncio>=0.23.0
ebooklib>=0.18.0
striprtf>=0.0.26
odfpy>=1.4.0
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=1.0.0
pydantic>=2.0.0
beautifulsoup4>=4.12.0
PyMuPDF>=1.23.0
reportlab>=4.0.0
numpy>=1.24.0
docx2python>=2.0.0
markitdown>=0.0.1
networkx>=3.0.0
# Tier 1 CPU test deps — needed by tests/test_domain9_embed_wiring.py and
# tests/test_excel_master.py / test_formula_validation.py / test_router.py.
# These are NOT GPU-only: the wiring tests assert the import guard is True,
# and the Excel formula tests call the `formulas` package for semantic eval.
# Without these, Tier 1 CI fails with "No module named 'embed_anything'/'formulas'".
embed-anything>=0.7.0
formulas>=1.3.0
psutil>=5.9.0
# GUI automation deps — pyscreeze and Pillow required by pyautogui on Python 3.12
# Without these, every scenario logs "PyAutoGUI was unable to import pyscreeze"
pyscreeze>=1.0.0
Pillow>=10.0.0
pyautogui>=0.9.54
