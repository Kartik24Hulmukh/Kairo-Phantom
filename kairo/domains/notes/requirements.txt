# No external dependencies required — pure stdlib markdown vault engine.
# markdown-it-py (MIT) is OPTIONAL for richer rendering; the core engine
# uses only Python stdlib (re, pathlib, dataclasses).
